import Ember from 'ember';

export default Ember.Object.extend({
    filter(key, schemaObj) {
        // get a reference to the specified schema with:
        // let fieldOption = schemaObj.options.fields[key];
        // once you have a reference you can manipulate how you want.
    }
});
