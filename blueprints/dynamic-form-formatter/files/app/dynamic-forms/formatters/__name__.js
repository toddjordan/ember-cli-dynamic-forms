import Ember from 'ember';

export default Ember.Object.extend({
    format() {
        // use let valueToFormat = this.getValue();
        // and then
        // this.setValue(formattedValue);
    }
});
